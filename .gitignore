# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
bun-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Staged native libs (populated by src-tauri/build.rs for the Tauri bundler)
src-tauri/native/

# CI-only Azure Trusted Signing overlay config (generated per-run by
# scripts/configure-windows-signing.sh; carries no secrets but isn't committed)
src-tauri/tauri.signing.conf.json

# Bun runtime cache
.bun/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
